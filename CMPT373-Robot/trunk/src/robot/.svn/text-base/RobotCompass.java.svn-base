package robot;

/**
 * RobotCompass Class
 * <P> Allows the Robot to get its current facing direction and store it in a register.
 */
public class RobotCompass extends RobotPart {
	int compassRegister;	//register for storing the facing direction of the robot
	
	public RobotCompass(){
		//for now, compasses cost $1 and weigh 1g
		super(1,1);
	}
	
	
	public int getCompassDirection(){
		//get the direction that the robot is facing from the board, store it in the compassRegister and return it.
		System.out.println("COMPASS instruction not yet implemented");
		return 0;
	}
}
