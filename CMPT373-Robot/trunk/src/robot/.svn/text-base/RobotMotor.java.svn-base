package robot;

/**
 * RobotMotor Class
 * <P> Allows the Robot to move around the Board. 
 */
public class RobotMotor extends RobotPart {
	public RobotMotor(){
		//for now let's just say all motors cost $1 and weigh 50g
		super(1,50);
	}
	
	public boolean move(int dist){
		//replace this with necessary code to update the robot's position on the board.
		System.out.println("MOVE instruction. Robot moves forward " + dist + " tile(s)");
		return true;
	}
}
