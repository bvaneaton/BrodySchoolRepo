package robot;
import board.*;

/**
 * RobotCamera Class
 * <P> Allows the Robot to "look" at the object dead ahead and store it in a register.
 */
public class RobotCamera extends RobotPart {
	MovableObject objectRegister;	//register for storing "seen" objects.
	
	public RobotCamera(){
		//for now, all cameras cost $1 and weigh 5g
		super(1,5);
	}
	
	public boolean look(){
		//replace this with necessary code to "look" ahead and place the object that is dead ahead in the objectRegister.
		System.out.println("LOOK instruction not yet implemented.");
		return true;
	}
}
