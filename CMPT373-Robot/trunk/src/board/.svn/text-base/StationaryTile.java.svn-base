package board;

class StationaryTile extends Tile {

	/*
	 * attribute of tiles
	 * true if the robot can move onto the tile unhindered, as in empty tiles
	 * false if the tile stops the robot, as in walls
	 */
	protected boolean passable;
	
	
	public boolean isPassable(){
		return passable;
	}
}
