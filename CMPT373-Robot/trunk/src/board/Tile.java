package board;

/*
 * This class exists only to bridge the gap between movable objects and stationary tiles. 
 * The layer functionality makes it necessary for tiles and objects to inherit from the same superclass, so this is that superclass.
 * The only thing it will contain is any methods shared by both stationary tiles and movable objects.
 */
class Tile {

}
